<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
        <meta name="application-name" content="{{ $appsettings->name() }} {{ $appsettings->version('v') }}">
        <meta name="keywords" content="@yield('meta-keywords', $appsettings->name())">
        <meta name="description" content="@yield('meta-description', $appsettings->name())">
        <meta name="robots" content="@yield('meta-robots', 'index,follow')">
        @yield('meta-tags', '')

        <title>@yield('pagetitle', $appsettings->name()) - {{ $appsettings->name() }}</title>

        {!! $style->render() !!}

        <script type="application/javascript">
            let _siteUrl = "{{ $config->get('app.url') }}";
            let _csrfToken = "{{ csrf_token() }}";
            let _locale = "{{ $config->get('app.locale') }}"
        </script>

        {!! $scripts->render() !!}

    </head>
    <body>
        <a id="top"></a>
        <div class="ui container" id="wrapper">
            <div id="header-wrapper">
                @include('layout.header')
            </div>
            <div id="main-menu-wrapper">
                @include('layout.mainmenu')
            </div>
            <div id="content-wrapper">
                @include('layout.loginnotice')

                @yield('content')
            </div>
        </div>
        <div id="footer-wrapper">
            @include('layout.footer')
        </div>

        {!! $scripts->render(['head' => false]) !!}
        @stack('inlinejs')

    </body>
</html>
